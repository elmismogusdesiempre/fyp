# FP Mensajería — Landing Vercel

Versión corregida. La imagen del hero utiliza mezcla `multiply` y una máscara radial para integrarse con el fondo blanco, evitando el efecto de rectángulo/parche. También se eliminó el `drop-shadow` que podía proyectar la silueta rectangular completa del PNG.
